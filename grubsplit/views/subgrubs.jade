extends layout

block content
  .page-header
    h1 Welcome to GrubSplit!
    h3 This is a subgrub's page
    //include all restaurant's info at top of page
    //display menu

    //display order panel on the right side - that's where the submit grub button shows.
    //	The panel moves with the scrolling of the page
  a(href="/grubs/0")
    button Add items to Grub

//- TODO: make the get request into a post
//-   form.form-horizontal(method='POST')
//-     a(href="/grubs/0")
//-       button.btn.btn-primary(type='submit') Add items to Grub
    